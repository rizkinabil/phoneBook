import React from "react";
import { Space, H1 } from "cherry-components";

const Header = () => {
	return (
		<>
			<Space size={20} />
			<H1>PhoneBook</H1>
			<Space size={20} />
		</>
	);
};

export default Header;
